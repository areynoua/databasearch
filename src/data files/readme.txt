To generate some random files

compile the project with the makefile and then try:
./stream generate [nbFiles] [minQuantity] [maxQuantity]

where : -nbFiles is the number of files you want to generate
        -minQuantity is the minimum quantity of integer that will be generated in the file
        -maxQuantity is the maximum quantity "" "" ""


These file are used for the benchmark of the streams (input)
